# Mobile app

Expo SDK 57 + Expo Router. Routes live in `src/app/`:

- `(auth)/`: sign in and sign up
- `(tabs)/`: Create, Batch (`index.tsx`), Cuts, Profile
- `cut/[id].tsx`: preview a finished edit and save it to the camera roll

See the [root README](../../README.md) for setup.
